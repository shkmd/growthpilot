'use client';
import {Dialog,DialogContent,DialogTitle,DialogDescription} from '@/components/ui/dialog';
export const widgetOptions=[['scores','Score tiles'],['overview','Audit overview and charts'],['findings','Findings and recent activity'],['images','Website images']] as const;
export function AddWidgetDrawer({open,onOpenChange,visible,onChange}:{open:boolean,onOpenChange:(open:boolean)=>void,visible:Record<string,boolean>,onChange:(key:string,value:boolean)=>void}){return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="db-widget-drawer"><DialogTitle>Add widgets</DialogTitle><DialogDescription>Choose which sections to show on your dashboard. Widgets use your selected website’s saved data.</DialogDescription>{widgetOptions.map(([id,label])=><label key={id}><input type="checkbox" checked={visible[id]} onChange={e=>onChange(id,e.target.checked)}/><span>{label}</span></label>)}<button onClick={()=>onOpenChange(false)}>Done</button></DialogContent></Dialog>}

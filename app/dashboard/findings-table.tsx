export function FindingsTable({issues,onReview,onView,hasAudit}:{issues:any[],onReview:(issue:any)=>void,onView:()=>void,hasAudit:boolean}){return <section className="db-card"><div className="db-card-head"><h2>Priority findings</h2><button onClick={onView}>View audit →</button></div><div className="db-findings"><table><thead><tr>{['Finding','Category','Severity','Pages'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{issues.slice(0,5).map((i,index)=><tr key={index}><td><button onClick={()=>onReview(i)}>{i.title}</button></td><td>{i.category||'On-page HTML'}</td><td><span className={'suite-severity '+i.severity}>{i.severity}</span></td><td><button onClick={()=>onReview(i)}>{i.urls.length} pages →</button></td></tr>)}</tbody></table></div>{!issues.length&&<p className="db-empty-note">{hasAudit?'No findings in this saved crawl. Review coverage before drawing conclusions about the whole website.':'Run an audit to see observed findings.'}</p>}{issues.length>5&&<button onClick={onView}>View all {issues.length} findings →</button>}</section>}

import {sqliteTable,text,integer,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
export const workspaceRecords=sqliteTable('workspace_records',{id:text('id').primaryKey(),owner:text('owner').notNull(),projectId:text('project_id').notNull().references(()=>projects.id),kind:text('kind').notNull(),data:text('data').notNull(),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull()},t=>[index('records_owner_project').on(t.owner,t.projectId)]);
export const projects=sqliteTable('projects',{id:text('id').primaryKey(),owner:text('owner').notNull(),name:text('name').notNull(),url:text('url').notNull(),country:text('country').notNull(),language:text('language').notNull(),createdAt:text('created_at').notNull()},t=>[index('projects_owner').on(t.owner)]);
export const audits=sqliteTable('audits',{id:text('id').primaryKey(),projectId:text('project_id').notNull().references(()=>projects.id),owner:text('owner').notNull(),createdAt:text('created_at').notNull(),score:integer('score').notNull(),result:text('result').notNull()},t=>[index('audits_owner_project_date').on(t.owner,t.projectId,t.createdAt)]);
export const tasks=sqliteTable('tasks',{id:text('id').primaryKey(),owner:text('owner').notNull(),projectId:text('project_id').notNull().references(()=>projects.id),title:text('title').notNull(),priority:text('priority').notNull(),url:text('url').notNull(),status:text('status').notNull().default('Open'),createdAt:text('created_at').notNull()},t=>[index('tasks_owner_project').on(t.owner,t.projectId),uniqueIndex('tasks_deduplicate').on(t.projectId,t.title)]);
export const auditLimits=sqliteTable('audit_limits',{owner:text('owner').primaryKey(),lastStarted:integer('last_started').notNull(),day:text('day').notNull(),count:integer('count').notNull()});

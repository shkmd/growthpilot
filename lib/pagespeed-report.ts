export function normalizePageSpeed(d:any,strategy:'mobile'|'desktop') {
 const l=d.lighthouseResult;
 if(!l?.audits||l.runtimeError)return {status:'unavailable',reason:l?.runtimeError?.message||'Google returned no Lighthouse report.'};
 const a=l.audits, number=(v:unknown)=>typeof v==='number'&&Number.isFinite(v)?v:null;
 const categories=Object.entries(l.categories||{}).map(([id,c]:[string,any])=>({id,title:c.title,score:number(c.score)===null?null:Math.round(c.score*100),audits:(c.auditRefs||[]).map((ref:any)=>a[ref.id]).filter(Boolean).map((x:any)=>({id:x.id,title:x.title,description:x.description,score:number(x.score),mode:x.scoreDisplayMode,displayValue:x.displayValue||'',savingsMs:number(x.details?.overallSavingsMs),savingsBytes:number(x.details?.overallSavingsBytes)}))}));
 const field=d.loadingExperience?.metrics?d.loadingExperience:null;
 return {status:'available',source:'Google PageSpeed Insights',strategy,score:categories.find(c=>c.id==='performance')?.score??null,categories,url:l.finalDisplayedUrl||l.finalUrl||d.id,measuredAt:l.fetchTime||d.analysisUTCTimestamp||new Date().toISOString(),version:l.lighthouseVersion,warnings:l.runWarnings||[],lcp:number(a['largest-contentful-paint']?.numericValue),cls:number(a['cumulative-layout-shift']?.numericValue),fcp:number(a['first-contentful-paint']?.numericValue),tbt:number(a['total-blocking-time']?.numericValue),speedIndex:number(a['speed-index']?.numericValue),ttfb:number(a['server-response-time']?.numericValue),field:field?{url:field.id,metrics:field.metrics}:null,note:'Lighthouse is a simulated page test. Field data, when available, describes real visitors. Scores can vary between runs.'};
}
